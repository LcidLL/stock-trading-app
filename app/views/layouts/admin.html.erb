<!DOCTYPE html>
<html>
<head>
  <title>Admin Dashboard</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <style>
    /* Basic styling for admin views - make it look good later */
    body { font-family: sans-serif; margin: 20px; }
    nav a { margin-right: 15px; }
    .field { margin-bottom: 10px; }
    .field label { display: block; font-weight: bold; }
    .field input[type="text"], .field input[type="email"], .field input[type="password"] {
      width: 300px; padding: 8px; border: 1px solid #ccc; border-radius: 4px;
    }
    .actions input[type="submit"] { padding: 10px 20px; background-color: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer; }
    .flash { padding: 10px; margin-bottom: 15px; border-radius: 4px; }
    .flash.notice { background-color: #d4edda; color: #155724; border-color: #c3e6cb; }
    .flash.alert { background-color: #f8d7da; color: #721c24; border-color: #f5c6cb; }
    #error_explanation { background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; padding: 10px; margin-bottom: 15px; border-radius: 4px; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
    th { background-color: #f2f2f2; }
    .button { display: inline-block; padding: 8px 12px; background-color: #28a745; color: white; text-decoration: none; border-radius: 4px; }
  </style>
  <%= google_visualr_js %>
</head>
<body>
  <header>
    <h1>Admin Panel</h1>
    <nav>
      <%= link_to 'Admin Dashboard', '/admin' %>
      <%= link_to 'Manage Users', admin_users_path %>
      <%= link_to 'Back to Main App', root_path %>
      <% if current_user %>
        <%= button_to 'Sign Out', destroy_user_session_path, method: :delete %>
      <% end %>
    </nav>
  </header>

  <% if notice %>
    <p class="flash notice"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="flash alert"><%= alert %></p>
  <% end %>

  <%= yield %>

</body>
</html>